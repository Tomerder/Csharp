<UserControl x:Class="Todo.App.Modules.TodoItem.EditTodoItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Todo.App.Modules.TodoItem"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300"
             d:DataContext="{StaticResource dt.EditTodoItemVm}"
             >
    <Grid Background="LightCoral">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding Uid}"/>
        <TextBlock Grid.Row="1" Text="{Binding DisplayName}"/>
        <TextBlock Grid.Row="2" Text="{Binding Priority}"/>
        <TextBlock Grid.Row="3" Text="{Binding Notes}" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
